type: object
description: Receipt stored when a token expires.
allOf:
  - $ref: "./ReceiptDTO.yml"
  - type: object
    required:
      - artifactId
    properties:
      artifactId:
        $ref: "../../../schemas/TokenId.yml"

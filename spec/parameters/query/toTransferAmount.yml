name: toTransferAmount
in: query
description: |
  Requires providing the `transferTokenId` filter.
  Only transfer transactions with a transfer amount of the provided token id, lesser or equal than this amount are returned.
schema:
  $ref: "../../schemas/Amount.yml"

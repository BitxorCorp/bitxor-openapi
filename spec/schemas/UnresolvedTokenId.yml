type: string
format: hex
description: |
  Token identifier. If the most significant bit of byte 0 is set, a namespaceId (alias)
  is used instead of the real token identifier.
example: 85BBEA6CC462B244

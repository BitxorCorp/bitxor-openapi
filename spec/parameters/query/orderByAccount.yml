name: orderBy
in: query
description: |
  Sort responses by the property set.
  If ``balance`` option is selected, the request must define the ``tokenId`` filter.
schema:
  $ref: "../../core/account/schemas/AccountOrderByEnum.yml"
  default: id
